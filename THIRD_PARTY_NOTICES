Tamashii — Third-Party Notices
==========================

This file records the third-party components used by Tamashii's Live2D rendering
pipeline, their versions as pinned in this repository, and their licenses.
See `docs/superpowers/specs/2026-07-20-live2d-renderer-design.md` §3 and §13
for the product/licensing decisions this list supports.

This file does not itself constitute legal advice or a completed license
audit for public distribution — see the "Live2D Cubism Core" section below
for the hard gate that still applies before any public release build may
ship.


1. PixiJS
---------
Version:  8.19.0 (pinned exactly in package.json, no `^` float — see design
          doc §13)
License:  MIT
Source:   https://github.com/pixijs/pixijs
Used for: WebGL rendering engine underlying the Live2D pet renderer
          (`Live2DPetRenderer` in `src/renderer/live2dRenderer.ts`).


2. untitled-pixi-live2d-engine
------------------------------
Version:  1.3.5 (pinned exactly in package.json — see design doc §13, and
          the Phase 4 pre-spike findings in design doc §17.2 for the
          version-compatibility caveats with Cubism Core below)
License:  MIT
Source:   https://www.npmjs.com/package/untitled-pixi-live2d-engine
Used for: The community PixiJS↔Cubism integration layer that loads and
          drives `*.model3.json` Live2D models inside a Pixi `Application`
          (imported via its `./cubism` subpath only, not the default export
          — see design doc §17.2 for why: the default export unconditionally
          pulls in legacy Cubism 2 runtime code this project does not use).
Note:     Design doc §17.2 records one version-compatibility bug found
          during the Phase 4 pre-spike: this engine version's compiled
          output assumes Cubism Core exposes a public
          `drawables.renderOrders` field, but the specific Cubism Core
          build in this repo (see below) makes that field private,
          reachable only via `getRenderOrders()`. A runtime compatibility
          patch for this is implemented in
          `src/renderer/live2dCubismCoreCompat.ts`.


3. Live2D Cubism Core (`live2dcubismcore.js`)
---------------------------------------------
Version:      Cubism Core 5, build 06.00.0001 (Cubism SDK for Web 5-r.5),
              per the version identified during the Phase 4 pre-spike
              (design doc §17.2). Not npm-distributed — downloaded directly
              from the Live2D Cubism SDK for Web and placed at
              `src/renderer/public/live2dcubismcore.js` (gitignored; not
              committed to this repository, not present in a fresh clone —
              see `src/renderer/public/.gitignore`).
SHA-256:      3ba426cc788d74f276598e98fe97983214f2a95758b48dafb1e606d041db1ba7
              (of the copy present on the primary development machine as of
              2026-07-22; re-verify this hash whenever the file is
              re-downloaded, since Live2D does not publish per-release
              checksums).
License:      Live2D Proprietary Software License
              (https://www.live2d.com/eula/live2d-proprietary-software-license-agreement_en.html)
              — NOT MIT/BSD/Apache. Embedded verbatim at the top of the
              file itself:
              "Copyright(c) Live2D Inc. All rights reserved. Use of this
              source code is governed by the Live2D Proprietary Software
              license that can be found at
              https://www.live2d.com/eula/live2d-proprietary-software-license-agreement_en.html."
Used for:     The official Cubism model runtime (MOC parsing, parameter
              evaluation, drawable/mask computation) that
              `untitled-pixi-live2d-engine` drives.

**Public-distribution gate (design doc §3):** per Live2D's SDK license,
shipping this file inside a publicly-distributed application (the
"Expandable Application" scenario Tamashii's product design falls under)
requires Live2D's written approval and a signed publisher agreement before
a public release build may include it. This is Phase 8 of the Live2D
rendering roadmap (design doc §16) and has not happened yet — do not build
or distribute a public installer that bundles this file until Phase 8 is
complete. Development, internal proof-of-concept, and local testing (this
repository's current state) are explicitly permitted without that approval
per Live2D's SDK license terms — see design doc §3 for the full reasoning
and official reference links.


4. Scope note
-------------
This list covers the Live2D rendering stack specifically (the subject of
`docs/superpowers/specs/2026-07-20-live2d-renderer-design.md`). Tamashii has
other third-party runtime dependencies (Electron, its voice TTS backends,
etc.) tracked via `package.json` / `pnpm-lock.yaml` and their own
upstream licenses (all permissive — MIT/Apache/BSD-family — as of this
writing); this file does not attempt to enumerate the full dependency
tree, only the components specific to the Live2D licensing decision this
design doc addresses.
